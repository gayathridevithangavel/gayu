pipeline {
    agent any

    stages {
        
        stage('build') {
            steps {
                echo 'building the app'
            }
        }
        
       stage('test') {
            steps {
                echo 'running the test' 
      }
}
       
       stage('deploy') {
        steps {
                echo 'deploying the app'
             }
        }
        
        
    }
}
